#!/usr/bin/env php
<?php

require_once "vendor/autoload.php";

use Blugen\App;
use Blugen\Command\Clear;
use Blugen\Command\Generate;

\Blugen\Container::get();

$app = new App('blugen');
$app->add(new Generate());
$app->add(new Clear());
$app->run();
